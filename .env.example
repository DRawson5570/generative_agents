# Example environment variables for Generative Agents
# Do NOT commit your real API keys. Copy this file to `.env` and fill in your keys.

# OpenAI API key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: name of the key owner (for bookkeeping)
KEY_OWNER="Your Name"

# Django/dev server
DJANGO_SETTINGS_MODULE=environment.frontend_server.settings

# Any other env vars you need for local runs can go here

# ---- Ollama (local LLM) - optional -------------------------------------------------
# Use a local Ollama server and model (7/8B models are common).
# Set to 'ollama' to route generation calls to Ollama.
LLM_BACKEND=ollama
OLLAMA_API_URL=http://localhost:11434/api/generate
OLLAMA_MODEL=llama3.1:8b

# ---- GitHub Copilot (optional) ---------------------------------------------------
# If you use GitHub Copilot as a provider, you can set a GitHub token which the
# app will exchange for a Copilot token and derived API base URL.
COPILOT_GITHUB_TOKEN=
COPILOT_DEFAULT_MODEL=grok-code-fast-1
